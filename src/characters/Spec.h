#include <string>
#include "Resource.h"

class Spec
{
private:
    /* data */
public:
    Spec(std::string name, Resource resouceUsed);
    ~Spec();
};

Spec::Spec(std::string name, Resource resouceUsed)
{
}

Spec::~Spec()
{
}
